[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
exclude = ["Color Detect.ipynb", "*.ipynb", ".venv", "venv", "build", "dist"]
line-length = 88
